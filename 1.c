#include <stdio.h>

int main()
{
    int i = 1;
    int year;
    float changes;
    int merge;
    int newbranch1;
    scanf("%d", &year);
    if(year%4 == 0 && year%100 != 0 || year%400 == 0)
    {
        printf("Yes");
    }
    else
    {
        printf("NO");
    }
    printf("CHANGES FOR STASH");
    printf("HARDRESET");
    while(i=1){
        printf("patch");
    }
}
